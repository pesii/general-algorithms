#ifndef ALGORITHMS

#define ALGORITHMS
#include <stdio.h>
#include <strings.h>

/*
 * returns 0 if not an anagram or 1 if it is an anagram
 */
int is_anagram(char *string);

/*
 * 
 */
int is_palindrome(void);

#endif
